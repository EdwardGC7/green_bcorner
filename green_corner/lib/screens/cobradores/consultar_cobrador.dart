import 'package:flutter/material.dart';

class ConsultarCobradorScreen extends StatelessWidget {
  const ConsultarCobradorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConsultarCobradorScreen'),
      ),
    );
  }
}
