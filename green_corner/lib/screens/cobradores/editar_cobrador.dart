import 'package:flutter/material.dart';

class EditarCobradorScreen extends StatelessWidget {
  const EditarCobradorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('EditarCobradorScreen'),
      ),
    );
  }
}
