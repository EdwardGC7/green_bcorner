import 'package:flutter/material.dart';

class PrestamosXCobradorScreen extends StatelessWidget {
  const PrestamosXCobradorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PrestamosXCobradorScreen'),
      ),
    );
  }
}
