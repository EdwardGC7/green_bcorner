import 'package:flutter/material.dart';

class TraspasoDClientesScreen extends StatelessWidget {
  const TraspasoDClientesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('TraspasoDClientesScreen'),
      ),
    );
  }
}
