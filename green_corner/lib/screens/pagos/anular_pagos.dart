import 'package:flutter/material.dart';

class AnularPagosScreen extends StatelessWidget {
  const AnularPagosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AnularPagosScreen'),
      ),
    );
  }
}
