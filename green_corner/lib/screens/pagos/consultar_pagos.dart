import 'package:flutter/material.dart';

class ConsultarPagosScreen extends StatelessWidget {
  const ConsultarPagosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConsultarPagosScreen'),
      ),
    );
  }
}
