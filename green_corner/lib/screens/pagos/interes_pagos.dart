import 'package:flutter/material.dart';

class InteresPagosScreen extends StatelessWidget {
  const InteresPagosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('InteresPagosScreen'),
      ),
    );
  }
}
