import 'package:flutter/material.dart';

class RecibosCobrarPagosScreen extends StatelessWidget {
  const RecibosCobrarPagosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('RecibosCobrarPagosScreen'),
      ),
    );
  }
}
