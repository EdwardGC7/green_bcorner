import 'package:flutter/material.dart';

class ReimpresionPagosScreen extends StatelessWidget {
  const ReimpresionPagosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ReimpresionPagosScreen'),
      ),
    );
  }
}
