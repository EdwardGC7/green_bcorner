import 'package:flutter/material.dart';

class AnularPrestamoScreen extends StatelessWidget {
  const AnularPrestamoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AnularPrestamoScreen'),
      ),
    );
  }
}
