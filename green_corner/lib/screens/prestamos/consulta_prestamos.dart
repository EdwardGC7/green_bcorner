import 'package:flutter/material.dart';

class ConsultaPrestamoScreen extends StatelessWidget {
  const ConsultaPrestamoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ConsultaPrestamoScreen'),
      ),
    );
  }
}
