import 'package:flutter/material.dart';

class ModificarPrestamoScreen extends StatelessWidget {
  const ModificarPrestamoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ModificarPrestamoScreen'),
      ),
    );
  }
}
